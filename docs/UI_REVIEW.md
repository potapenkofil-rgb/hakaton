# Точечное обновление: логотип, tooltip, прокрутка

Выполнен второй план пользователя; предыдущий отчёт ниже сохранён как история.

- `app/static/assets/kosmokontur-logo.png`: точная копия последнего PNG из
  Downloads (`e4f9169f-cfdf-4ee5-8601-5b76813da6ba.png`), 2508×627.
  SHA256 оригинала и копии: `0376756E9C59A9F19FB0A380500C353B731ADF352555428FCF2E73A8381C9738`.
- `index.html`: изображение заменило SVG и дублирующий текст; favicon использует
  тот же PNG. Добавлено сообщение об ошибке около нижней кнопки, уточнена
  подсказка управления графиком.
- `styles.css`: локальное размещение PNG без обрезки, эффектов и изменения
  пропорций; focus-обводка отдельных элементов графика.
- `app.js`: custom SVG tooltip привязан к отдельному столбцу и содержит только
  год, показатель и значение в тоннах. Существующие три серии и их значения
  сохранены; надпись дефицита также имеет отдельную подсказку. Закрепление года
  и подсветка баланса сохранены, закрепление не удерживает tooltip вне столбца.
  Высота подсказки определяется содержимым, позиция привязана к элементу.
- Обе кнопки «Посчитать» вызывают плавный scrollIntoView к существующему
  `#overview` после render. У секции уже был scroll-margin-top: 20px.
  Ошибка расчёта не вызывает прокрутку, детали дополнительно видны у формы.
  Автоматические пересчёты при смене сценария не получили нового автоскролла.
- `tools/ui_smoke.cjs`: обновлены проверки отдельных tooltip, промежутков между
  столбцами, пропорций PNG; добавлены проверки успешной прокрутки после render,
  сохранения параметров, отсутствия прокрутки при INVALID_PLAN и искусственном
  ответе 500 от сервера. Проверяется положение кнопки на экране: штатный scroll
  anchoring браузера может корректировать scrollY при появлении текста выше.

Проверки: 272 pytest-теста прошли (5,25 с), браузерный smoke-test полностью
прошёл; JSON upload/download, save/reload, CSV/XLSX, overrides/reset, BASE,
MANDATORY_STRESS, TEAM_HIGH_DEMAND, response, invalid и broken работают.
Числа tooltip сверены с backend. Неожиданных console/page errors нет;
400 и искусственный 500 в ошибочных тестах ожидаемы. `git diff --check` и
`node --check app/static/app.js` проходят. Backend/API/схемы/данные не менялись.

Проверены ширины 1024, 1280, 1366, 1440, 1920; скриншоты и JSON-отчёт находятся
в `results/exports/ui-review/`. PNG не обрезан, включая исходные светлые поля;
мелкая подпись является частью изображения. Дополнительно рекомендуется
оценить её читаемость на экране/проекторе защиты при реальном масштабе браузера.
Полный автопрогон — Chrome; Firefox/Safari отдельно не проверялись.
Commit/push не выполнялись. Все изменения остаются в `review-main`.

---

# Проверка frontend — 19 сентября 2026

Работа выполнена в `review-main`, отдельном клоне указанного репозитория,
на ветке `main`, исходный коммит `e4a1843781dd28bc51618200cc8b55ef5b336269`.
Исходная папка пользователя была архивной копией без `.git`.
Изменения не закоммичены, push/merge не выполнялись.

## Выполнение плана

1. Удалены служебная подпись sidebar и обращение JS к удалённому `#engine`.
2. График спроса: custom tooltip с годом, спросом, фактическими приходом и
   выдачей, дефицитом и тоннами; hover/focus, выбор года кликом/Enter/Space,
   снятие выбора Escape, выделение строки баланса. Подписи «Дефицит … т».
3. Легенда разбита на цветовые обозначения с полными названиями. Все серии
   остаются видимыми; необязательное переключение серий не добавлялось.
4. Заголовки поставок раскрывают смысл всех backend-полей и единицы измерения.
5. `formatOrderBy`: месяц/год, «до начала 2035», полный исходный текст в title,
   прочие строки сохранены и экранированы, пустое значение — тире.
6. Технические таблицы получили фиксированную раскладку, одинаковые числовые
   поля, переносы заголовков/примечаний, локальный scroll и sticky названия.
   Выравнивание ячеек теперь определяется колонкой, а не длиной текста.
7. Снята максимальная ширина content. Проверены 1280, 1366, 1440 и 1920 px.
8. Сравнение занимает всю ширину: сначала две карточки с исполнимостью, PV,
   общими затратами, CAPEX, дефицитом, критическим сервисом; затем изменение
   ключевых метрик и подробная таблица. Стоимость не маркируется красным.
   Разница сервиса выражена в процентных пунктах. Устаревшее сравнение
   скрывается при изменении исходных параметров/новом расчёте.
9. Усилены фон, границы, заголовки, secondary text, шапки таблиц, hover/focus;
   основной текст остаётся 14 px, подписи графиков увеличены.
10. Знак адаптирован из `docs/logo/square/3-datchik.svg`: без тёмного квадрата,
    с палитрой интерфейса; название сохранено. Добавлен встроенный favicon.
11. Существующие действия сохранены. Дополнительно исправлена потеря ошибок
    неизвестных каналов/годов при загрузке: исходный JSON проверяется текущим
    `/api/calculate` до заполнения редактора.
12. Backend, API, формулы, сценарии и исходные данные не изменены.

## Проверки

- `.venv/Scripts/python.exe -m pytest`: **272 passed**, последний прогон 4,12 с.
- `node --check app/static/app.js`: успешно.
- `git diff --check`: успешно.
- `node tools/ui_smoke.cjs` с Playwright и Chrome: все проверки прошли.
  Для bundled runtime задать `NODE_PATH` на его `dependencies/node/node_modules`.
  Сервер: `PYTHONPATH=src`, `python -m fuelhub serve`.

Браузерный smoke-test проверяет:

- v3-earth BASE и MANDATORY_STRESS; v3-earth-response в обоих сценариях;
  TEAM_HIGH_DEMAND; base-v1 и stress-response; invalid и broken;
- оба графика и hover; demand tooltip, click, focus, Enter, Space, Escape,
  выделение строки баланса; formatter сроков, экранирование неизвестного текста;
- сравнение и совпадение отображаемого PV с ответом API;
- изменение заказа, пересчёт Enter, отключение/включение ZBO;
- overrides цены и спроса, ожидаемый PV 9282, reset;
- сохранение под отдельным тестовым именем и повторное открытие после reload;
- JSON download/upload, синтаксически повреждённый JSON и invalid JSON;
- скачивание CSV/XLSX; отсутствие переполнения страницы на четырёх ширинах;
- отсутствие uncaught JS и неожиданных console errors. Ответы 400 при
  намеренной загрузке невалидных планов ожидаемы и не считаются JS-ошибками.

Отдельно проверены CSV (870 строк, оба overrides с 7 и 420) и целостность ZIP
XLSX, наличие обоих overrides в XML. Тестовый сохранённый план удалён.

Вручную через открытый браузер проверены BASE, переключение в стресс,
карточки сравнения, ошибки invalid (отрицательный объём, канал F, год 2041),
broken, исполнимый ответ на стресс и визуальная раскладка данных кейса.
Просмотрены скриншоты обзора 1440, сравнения, tooltip и данных кейса.
Остальные действия покрыты автоматическим браузерным smoke-test.

Артефакты: `results/exports/ui-review/` (игнорируются Git): скриншоты четырёх
ширин, графика, сравнения, данных кейса; JSON-отчёт и тестовые выгрузки.

## Ограничения и расхождения документации

- Текущий backend для broken возвращает **12**, а не 11 нарушений из
  `docs/UI_TEST.md`. Подтверждено прямым запуском CLI; числа UI не подменялись.
- В актуальном v3-earth заказ Earth-Core 2035 равен **100**, хотя протокол
  редактирования описывает 95. Проверка изменения 200 и допустимого 95 проходит.
- Полный автопрогон сделан в Chrome; отдельной проверки Firefox/Safari нет.
- Широкие технические таблицы намеренно прокручиваются внутри панелей.
- Все изменения расположены в клоне `review-main`, не в исходной архивной копии.
